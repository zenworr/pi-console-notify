{
  "name": "pi-console-notify",
  "version": "0.2.0",
  "description": "Route console messages through Pi's notification UI.",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package"],
  "scripts": {
    "test": "node --test index.test.ts"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
